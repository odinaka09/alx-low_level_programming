#include "main.h"

/**
 * print_to_98 - prints all natural numbers fron n to 98
 * @n: number
 * Return: Always 0.
 */
void print_to_98(int n)
{
	while (n < 98)
	{
		if (n == 98)
		{
			printf("%d", n);
		}
		printf("%d", n);
		printf(",");
		printf(" ";)
		n++;
	}
	while (n > 98)
	{
		if (n == 98)
		{
			printf("%d", n);
		}
		printf("%d", n);
		printf(",");
		printf(" ";)
		n--;
	}
}
